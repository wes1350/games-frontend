import * as mocha from "mocha";
import { expect } from "chai";

describe("BoardViewTest", () => {
    it("should return a proper structure detailing domino locations", () => {
        expect(1 + 1).to.eq(2);
    });
});

expect(3).to.eq(2 + 1);
